services:
  - type: web
    name: mcp-justwatch
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && pip install .
    startCommand: python -m mcp_justwatch.server
    envVars:
      - key: MCP_BASE_URL
        value: https://mcp-justwatch.onrender.com
